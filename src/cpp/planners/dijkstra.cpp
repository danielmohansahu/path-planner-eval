#include "dijkstra.hpp"

PlanResult plan_dijkstra(const Grid &grid, Coord start, Coord goal,
                         int connectivity) {
  PlanResult result;
  result.solved = false;
  return result;
}
